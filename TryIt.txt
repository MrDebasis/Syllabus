.slds-table thead th {
    position: relative;
}

.slds-table thead th:hover::after {
    content: attr(data-tooltip);
    position: absolute;
    left: 0;
    top: 100%;
    background-color: #333;
    color: #fff;
    padding: 5px;
    border-radius: 5px;
    white-space: nowrap;
    z-index: 1;
}


import { LightningElement, track } from 'lwc';

export default class ParentComponent extends LightningElement {
    @track columns = [
        { 
            label: 'Notes', 
            fieldName: 'Notes__c', 
            sortable: true, 
            hideDefaultActions: true, 
            fixedWidth: 120, 
            wrapText: true,
            cellAttributes: { 
                class: {
                    fieldName: "rowClass"
                }
            },
            headerAttributes: {
                'data-tooltip': 'This column displays notes.' // Tooltip message for header
            }
        },
        // ... other columns
    ];

    @track data = [
        // Your data here
    ];
}
