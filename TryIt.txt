       @AuraEnabled(cacheable=true)
    public static List<ExpenseLineItem__c> getExpenseLineItems() {
        return [
            SELECT Id, ExpenseDate__c, ExpenseCategory__c, ExpenseDetailVendor__c, ExpenseLocation__c,ReceiptAmount__c, 
                   ReimbursableAmount__c,NonReimbursableAmount__c,Notes__c 
            FROM ExpenseLineItem__c 
            ORDER BY CreatedDate DESC 
            LIMIT 10
        ];
    }
