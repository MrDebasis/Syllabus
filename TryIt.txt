

                    if(numericValue.toString().substring(numericValue.length-1).contains('.')){
                        this.isReceiptAmountValid = false;
                        this.isReceiptAmountValidDynamicClass();
                        return;
                    }
