sortedCases.sort((a, b) => {
    const aIsEmpty = !a.NextAction__c && !a.NextActionDueDate__c;
    const bIsEmpty = !b.NextAction__c && !b.NextActionDueDate__c;

    if (aIsEmpty && !bIsEmpty) return -1;
    if (!aIsEmpty && bIsEmpty) return 1;
    return 0;
});
