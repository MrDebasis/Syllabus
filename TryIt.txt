
1: {bool: 'showMissingReceiptScreen', value: 'valuesFromMissingReceiptScreen', btnName: 'Add receipts', showBack: true}
2: {bool: 'showMissingReceiptReasonScreen', value: 'valuesFromReceiptReasonScreen', recordValue: Proxy(Object), btnName: 'Next', showBack: true}
4: {bool: 'showMissingReceiptReasonScreen', value: 'valuesFromReceiptReasonScreen', recordValue: Proxy(Object), btnName: 'Next', showBack: true}
6: {bool: 'submitExpense', value: 'valuesFromSubmitExpense', btnName: 'Submit expenses',
 showBack:: true}


            let categories = {};
            const baseIndex = 1;
            const submitExpenseIndex = (this.onlyMissingReceiptRecords.length * 2) + baseIndex + 1;

            /* initial screen */
            categories[baseIndex] = { 
                bool: 'showMissingReceiptScreen', 
                value: 'valuesFromMissingReceiptScreen', 
                btnName: 'Add receipts', 
                showBack: true 
            };

            for (let i = 0; i < this.onlyMissingReceiptRecords.length; i++) {
                let screenIndex = (i * 2) + baseIndex + 1;
                let sendMissingRecordToChild =  this.onlyMissingReceiptRecords[i];

                // Adding reason screen
                categories[screenIndex] = {
                    bool: 'showMissingReceiptReasonScreen',
                    value: 'valuesFromReceiptReasonScreen',
                    recordValue : sendMissingRecordToChild,
                    btnName: 'Next',
                    showBack: true
                };

                // Adding limit screen
                // categories[screenIndex + 1] = {
                //     bool: 'showMissingReceiptLimitScreen',
                //     value: 'valuesFromReceiptLimitScreen',
                //     recordValue : sendMissingRecordToChild,
                //     btnName: 'Next',
                //     showBack: true
                // };
            }

            // Adding the final submit expense screen
            categories[submitExpenseIndex] = {
                bool: 'submitExpense',
                value: 'valuesFromSubmitExpense',
                btnName: 'Submit expenses',
                showBack: true
            };

            // Updating the dynamic screen to flowScreen
            this.flowScreens['Without Receipt Screens'] = categories;

            this.currentScreenIndex ++;
            this.closeScreens();
            this.updateScreen();
        }
