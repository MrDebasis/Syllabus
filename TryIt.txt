<template if:true={tripDatesSection}>
                <label>When should we check back in to confirm if bridging therapy will be needed?*</label>
                        Arrival Date     <lightning-input 
                    type="date"
                    data-id={q.apiName}
                    onchange={handleInputChange}
                    required={q.required}>
                </lightning-input>

                         dEPARTURE  Date     <lightning-input 
                    type="date"
                    data-id={q.apiName}
                    onchange={handleInputChange}
                    required={q.required}>
                </lightning-input>
                <div>OPEN DATE SECTION</div>
        </template>
