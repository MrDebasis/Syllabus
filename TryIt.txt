this.cases = this.cases.map(row => {

    if (row.Id === rowId) {

        // Create a new object with the same order as the original row

        const updatedField = Object.keys(updatedValues)[0]; // Get the field being updated

        const updatedRow = { ...row }; // Clone the original row
 
        // Update the specific field with the new value

        updatedRow[updatedField] = updatedValues[updatedField];
 
        return updatedRow; // Return the updated row

    }

    return row; // Return the row unchanged if it doesn't match the rowId

});
 
