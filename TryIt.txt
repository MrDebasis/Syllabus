case "nestedBetween":
            if (!value) return false;
            return value.some(item => {
                const valDate = new Date(item);
                const [start, end] = filter.value.map(v => new Date(v));
                return valDate >= start && valDate <= end;
            });
