 if (this.mobileView && Array.isArray(this.allFiles)) {
        // Corrected filter logic
        this.allFiles = this.allFiles.filter(file => {
            console.log('Checking ContentDocumentId:', file.ContentDocumentId);
            return this._contentDocumentIds.includes(file.ContentDocumentId);
        });

        console.log('2nd this.allFiles:', JSON.stringify(this.allFiles));
    }
