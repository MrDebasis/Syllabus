
formatDate(dateString) {
  const date = new Date(dateString);
  const isoDate = date.toISOString(); // Returns date in YYYY-MM-DDTHH:mm:ss.sssZ format
  return isoDate.split('T')[0]; // Extract just the date part
}
