import upsertMilestones from '@salesforce/apex/YourApexClassName.upsertMilestones';

const jsonString = JSON.stringify(payload);

        upsertMilestones({ payloadJson: jsonString, caseId: this.caseId, surveyType: this.surveyType })
            .then(() => {
                // Success message
                console.log('Milestones upserted successfully.');
            })
            .catch(error => {
                console.error('Error upserting milestones:', error.body.message);
            });
    }
