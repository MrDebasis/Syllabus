#!/bin/bash

# Extract arguments
arg1="$1"
arg2="$2"
arg3="$3"

# Check if Java is installed
if command -v java &> /dev/null; then
    echo "Java is installed on your system."
    java -version

    # Output the arguments received from the server
    echo "Received arguments: arg1=$arg1, arg2=$arg2, arg3=$arg3"
    
    # Perform any additional operations with the arguments
    # For example:
    # echo "Performing operation with arguments: $arg1 $arg2 $arg3"

    # Open Thymeleaf page with provided arguments
    # Replace this with your actual command to open the Thymeleaf page
    # java -jar path/to/thymeleaf.jar "$arg1" "$arg2" "$arg3"

    # For demonstration purposes, let's simply echo the arguments
    echo "arg1=$arg1, arg2=$arg2, arg3=$arg3"
else
    echo "Java is not installed on your system."
fi

