    const updatedData = data.map(item => {
                    return {
                        ...item,
                        isEmail: item.notificationType === 'Email' // Assuming 'Email' is the type to check
                    };
                });

                this.records = [...this.records, ...updatedData];
